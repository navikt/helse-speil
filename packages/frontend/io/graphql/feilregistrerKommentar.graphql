mutation FeilregistrerKommentarMutation($id: Int!) {
    feilregistrerKommentar(id: $id) {
        id
        opprettet
        feilregistrert_tidspunkt
        saksbehandlerident
        tekst
    }
}
