/*import { NextResponse } from 'next/server';

import { DialogMock } from '@spesialist-mock/storage/dialog';
import { NotatMock } from '@spesialist-mock/storage/notat';
import { VarselMock } from '@spesialist-mock/storage/varsel';

export const stub = async (_request: Request, params: Promise<{ kommentarId: string }>) => {
    const { kommentarId } = await params;
    const kommentar = DialogMock.addKommentar();
    return NextResponse.json(kommentar, { status: 200 });
};*/
