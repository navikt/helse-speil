query RESTGetAktiveSaksbehandlere {
    restGetAktiveSaksbehandlere
    @rest(
        type: "[Saksbehandler!]!"
        endpoint: "spesialist"
        path: "/aktive-saksbehandlere"
        method: "GET"
    ) {
        ident
        navn
    }
}