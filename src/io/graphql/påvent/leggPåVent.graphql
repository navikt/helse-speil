mutation LeggPaVent($oppgaveId: String!, $frist: LocalDate!, $tildeling: Boolean!, $begrunnelse: String, $notatTekst: String!) {
    leggPaVent(oppgaveId: $oppgaveId, frist: $frist, tildeling: $tildeling, begrunnelse: $begrunnelse, notatTekst: $notatTekst) {
        ...pavent
    }
}
